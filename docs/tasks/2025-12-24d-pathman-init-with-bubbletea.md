# Rewrite pathman init to use bubbletea

Now can we redesign the interaction in pathman init to use bubbletea? It only
asks a single question at the moment - because it only recognises bash - but
that may change in the future.